---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: postgresdatabases.databases.example.io
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
spec:
  group: databases.example.io
  names:
    kind: PostgresDatabase
    listKind: PostgresDatabaseList
    plural: postgresdatabases
    singular: postgresdatabase
    shortNames:
      - pgdb
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
        scale:
          specReplicasPath: .spec.replicas
          statusReplicasPath: .status.readyReplicas
      additionalPrinterColumns:
        - name: Phase
          type: string
          jsonPath: .status.phase
        - name: Ready
          type: integer
          jsonPath: .status.readyReplicas
        - name: Version
          type: string
          jsonPath: .spec.version
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          type: object
          description: PostgresDatabase is the Schema for the postgresdatabases API.
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              type: object
              description: PostgresDatabaseSpec defines the desired state of PostgresDatabase.
              properties:
                version:
                  type: string
                  description: Version of PostgreSQL to deploy.
                  default: "16"
                  enum: ["14", "15", "16", "17"]
                replicas:
                  type: integer
                  format: int32
                  description: Number of PostgreSQL pods (1 = standalone, 2+ = HA).
                  default: 1
                  minimum: 1
                  maximum: 5
                database:
                  type: string
                  description: Name of the initial database to create.
                  default: appdb
                username:
                  type: string
                  description: Owner of the initial database.
                  default: appuser
                storage:
                  type: object
                  description: PVC configuration for each pod.
                  required:
                    - size
                  properties:
                    size:
                      type: string
                      description: Storage request (e.g. "10Gi").
                      x-kubernetes-int-or-string: true
                    storageClassName:
                      type: string
                      description: StorageClass name (defaults to cluster default).
                resources:
                  type: object
                  description: CPU and memory for each PostgreSQL container.
                  properties:
                    limits:
                      type: object
                      additionalProperties:
                        x-kubernetes-int-or-string: true
                    requests:
                      type: object
                      additionalProperties:
                        x-kubernetes-int-or-string: true
                passwordSecretRef:
                  type: object
                  description: Reference to an existing Secret with the password.
                  required:
                    - name
                    - key
                  properties:
                    name:
                      type: string
                    key:
                      type: string
              required:
                - storage
                - version
            status:
              type: object
              description: PostgresDatabaseStatus defines the observed state.
              properties:
                phase:
                  type: string
                  description: "Current lifecycle phase: Pending, Running, or Failed."
                readyReplicas:
                  type: integer
                  format: int32
                secretName:
                  type: string
                serviceName:
                  type: string
                conditions:
                  type: array
                  items:
                    type: object
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        type: string
                        format: date-time
                      message:
                        type: string
                      observedGeneration:
                        type: integer
                        format: int64
                      reason:
                        type: string
                      status:
                        type: string
                      type:
                        type: string
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
